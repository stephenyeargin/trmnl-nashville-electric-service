# TRMNLP configuration
# {{ env.VARIABLE }} interpolation is available here
---
# auto-reload when files change (`watch: false` to disable)
watch:
  - .trmnlp.yml
  - src

# values of custom fields (defined in src/settings.yml)
custom_fields: {
  # thunderforest_api_key: '{{ env.THUNDERFOREST_API_KEY }}', # Doesn't work as of 7/21/25
  thunderforest_api_key: 'e43d4ad3df1846b58584aa02c7330c6b'
}

# override variables
variables:
  trmnl: {
    plugin_settings: {
      instance_name: 'Nashville Electric Service (NES)'
    }
  }
